import Quack

// The primary window: a full three-pane shell. Additional windows (independent
// shells and pop-out chats) are spawned at runtime via the AppWindows factory.
// All windows share the one backend and account models behind the App singleton.
ShellWindow {
}
